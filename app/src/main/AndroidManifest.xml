<?xml version="1.0" encoding="utf-8"?>
<!--suppress CheckTagEmptyBody -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.catalog.android.client"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17"
        tools:ignore="GradleOverrides,OldTargetApi" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddBottleActivity"
            android:label="@string/add_bottle"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".ShowBottleActivity"
            android:label="@string/add_bottle"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".ShowAllBottlesActivity"
            android:label="@string/show_all_bottles"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".DeleteBottleActivity"
            android:label="@string/delete_bottle"
            android:screenOrientation="portrait"></activity>
    </application>

</manifest>
